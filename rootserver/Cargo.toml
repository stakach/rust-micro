[package]
name = "rootserver"
version = "0.1.0"
edition = "2021"

[dependencies]

[profile.release]
panic = "abort"
opt-level = 2
lto = false
debug = false
codegen-units = 1
overflow-checks = false

[profile.dev]
panic = "abort"
