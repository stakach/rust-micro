From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Stephen von Takach <steve@place.technology>
Date: Mon, 31 Aug 2026 07:53:55 +1000
Subject: [PATCH] ACPI: enumerate provider namespace PDOs

---
 drivers/bus/acpi/CMakeLists.txt    |   1 +
 drivers/bus/acpi/acpienum.c        |  92 ++++++---
 drivers/bus/acpi/buspdo.c          |  22 +-
 drivers/bus/acpi/include/acpisys.h |  17 ++
 drivers/bus/acpi/main.c            |   8 +
 drivers/bus/acpi/namespace.c       | 317 +++++++++++++++++++++++++++++
 drivers/bus/acpi/pnp.c             |   6 +-
 7 files changed, 423 insertions(+), 40 deletions(-)
 create mode 100644 drivers/bus/acpi/namespace.c

diff --git a/drivers/bus/acpi/CMakeLists.txt b/drivers/bus/acpi/CMakeLists.txt
index f56afd97..2190b7a5 100644
--- a/drivers/bus/acpi/CMakeLists.txt
+++ b/drivers/bus/acpi/CMakeLists.txt
@@ -192,6 +192,7 @@ list(APPEND ACPI_SOURCE
     busmgr/system.c
     osl.c
     acpienum.c
+    namespace.c
     eval.c
     interface.c
     pnp.c
diff --git a/drivers/bus/acpi/acpienum.c b/drivers/bus/acpi/acpienum.c
index 1b8ec881..75875283 100644
--- a/drivers/bus/acpi/acpienum.c
+++ b/drivers/bus/acpi/acpienum.c
@@ -15,9 +15,66 @@
 #define HAS_CHILDREN(d)		((d)->children.next != &((d)->children))
 #define HAS_SIBLINGS(d)		(((d)->parent) && ((d)->node.next != &(d)->parent->children))
 #define NODE_TO_DEVICE(n)	(list_entry(n, struct acpi_device, node))
+#define ACPI_MAX_INSTANCE_ID_CHARS 199
 
 extern struct acpi_device		*acpi_root;
 
+static
+NTSTATUS
+Bus_AllocateInstanceId(
+    ACPI_HANDLE Handle,
+    PWCHAR *InstanceId)
+{
+    ACPI_BUFFER Path = {ACPI_ALLOCATE_BUFFER, NULL};
+    ACPI_STATUS AcpiStatus;
+    PCSTR Source;
+    SIZE_T SourceLength;
+    SIZE_T Index;
+    PWCHAR Buffer;
+
+    *InstanceId = NULL;
+    AcpiStatus = AcpiGetName(Handle, ACPI_FULL_PATHNAME, &Path);
+    if (ACPI_FAILURE(AcpiStatus) || !Path.Pointer)
+        return AcpiStatus == AE_NO_MEMORY ? STATUS_INSUFFICIENT_RESOURCES
+                                         : STATUS_INVALID_DEVICE_REQUEST;
+
+    Source = Path.Pointer;
+    SourceLength = strlen(Source);
+    if (SourceLength < 5 ||
+        SourceLength > ACPI_MAX_INSTANCE_ID_CHARS + 1 ||
+        Source[0] != '\\')
+    {
+        AcpiOsFree(Path.Pointer);
+        return STATUS_INVALID_DEVICE_REQUEST;
+    }
+    for (Index = 1; Index < SourceLength; Index++)
+    {
+        if ((UCHAR)Source[Index] < ' ' ||
+            (UCHAR)Source[Index] > 0x7f ||
+            Source[Index] == ',' ||
+            Source[Index] == '\\')
+        {
+            AcpiOsFree(Path.Pointer);
+            return STATUS_INVALID_DEVICE_REQUEST;
+        }
+    }
+
+    Buffer = ExAllocatePoolWithTag(PagedPool,
+                                   SourceLength * sizeof(WCHAR),
+                                   'IpcA');
+    if (!Buffer)
+    {
+        AcpiOsFree(Path.Pointer);
+        return STATUS_INSUFFICIENT_RESOURCES;
+    }
+    for (Index = 1; Index < SourceLength; Index++)
+        Buffer[Index - 1] = (WCHAR)(UCHAR)Source[Index];
+    Buffer[SourceLength - 1] = UNICODE_NULL;
+    AcpiOsFree(Path.Pointer);
+    *InstanceId = Buffer;
+    return STATUS_SUCCESS;
+}
+
 NTSTATUS
 Bus_PlugInDevice (
     struct acpi_device *Device,
@@ -41,38 +98,12 @@ Bus_PlugInDevice (
     for (entry = FdoData->ListOfPDOs.Flink;
         entry != &FdoData->ListOfPDOs; entry = entry->Flink)
     {
-        struct acpi_device *CurrentDevice;
-
         pdoData = CONTAINING_RECORD (entry, PDO_DEVICE_DATA, Link);
 
         //dont duplicate devices
         if (pdoData->AcpiHandle == Device->handle)
             return STATUS_SUCCESS;
 
-        //check everything but fixed feature devices
-        if (pdoData->AcpiHandle)
-            acpi_bus_get_device(pdoData->AcpiHandle, &CurrentDevice);
-        else
-            continue;
-
-        //check if the HID matches
-        if (!strcmp(Device->pnp.hardware_id, CurrentDevice->pnp.hardware_id))
-        {
-            //check if UID exists for both and matches
-            if (Device->flags.unique_id && CurrentDevice->flags.unique_id &&
-                !strcmp(Device->pnp.unique_id, CurrentDevice->pnp.unique_id))
-            {
-                /* We have a UID on both but they're the same so we have to ignore it */
-                DPRINT1("Detected duplicate device: %hs %hs\n", Device->pnp.hardware_id, Device->pnp.unique_id);
-                return STATUS_SUCCESS;
-            }
-            else if (!Device->flags.unique_id && !CurrentDevice->flags.unique_id)
-            {
-                /* No UID so we can only legally have 1 of these devices */
-                DPRINT1("Detected duplicate device: %hs\n", Device->pnp.hardware_id);
-                return STATUS_SUCCESS;
-            }
-        }
     }
 
     DPRINT("Exposing PDO\n"
@@ -103,6 +134,13 @@ Bus_PlugInDevice (
     pdoData = (PPDO_DEVICE_DATA) pdo->DeviceExtension;
 	pdoData->AcpiHandle = Device->handle;
 
+    status = Bus_AllocateInstanceId(Device->handle, &pdoData->InstanceId);
+    if (!NT_SUCCESS(status))
+    {
+        IoDeleteDevice(pdo);
+        return status;
+    }
+
     //
     // Copy the hardware IDs
     //
@@ -122,6 +160,8 @@ Bus_PlugInDevice (
 
 
     if (!pdoData->HardwareIDs) {
+        ExFreePoolWithTag(pdoData->InstanceId, 'IpcA');
+        pdoData->InstanceId = NULL;
         IoDeleteDevice(pdo);
         return STATUS_INSUFFICIENT_RESOURCES;
     }
diff --git a/drivers/bus/acpi/buspdo.c b/drivers/bus/acpi/buspdo.c
index 6beda9be..0adffa7c 100644
--- a/drivers/bus/acpi/buspdo.c
+++ b/drivers/bus/acpi/buspdo.c
@@ -414,7 +414,7 @@ Bus_PDO_QueryDeviceCaps(
        deviceCapabilities->HardwareDisabled = !device->status.enabled && !device->status.functional;
        deviceCapabilities->Removable = device->flags.removable;
        deviceCapabilities->SurpriseRemovalOK = device->flags.surprise_removal_ok;
-       deviceCapabilities->UniqueID = device->flags.unique_id;
+       deviceCapabilities->UniqueID = DeviceData->InstanceId != NULL;
        deviceCapabilities->NoDisplayInUI = !device->status.show_in_ui;
        deviceCapabilities->Address = device->pnp.bus_address;
     }
@@ -509,22 +509,18 @@ Bus_PDO_QueryDeviceId(
 
         /* This is a REG_SZ value */
 
-        /* See comment in BusQueryDeviceID case */
-        if(DeviceData->AcpiHandle)
+        if (DeviceData->InstanceId)
         {
-           acpi_bus_get_device(DeviceData->AcpiHandle, &Device);
-
-           if (Device->flags.unique_id)
-              length = _swprintf(temp,
-                                 L"%hs",
-                                 Device->pnp.unique_id);
-           else
-              /* FIXME: Generate unique id! */
-              length = _swprintf(temp, L"%ls", L"0");
+           length = wcslen(DeviceData->InstanceId);
+           if (length >= RTL_NUMBER_OF(temp))
+           {
+               status = STATUS_INVALID_DEVICE_REQUEST;
+               break;
+           }
+           wcscpy(temp, DeviceData->InstanceId);
         }
         else
         {
-           /* FIXME: Generate unique id! */
            length = _swprintf(temp, L"%ls", L"0");
         }
 
diff --git a/drivers/bus/acpi/include/acpisys.h b/drivers/bus/acpi/include/acpisys.h
index 3442e424..c9ba45a3 100644
--- a/drivers/bus/acpi/include/acpisys.h
+++ b/drivers/bus/acpi/include/acpisys.h
@@ -8,6 +8,16 @@ extern UNICODE_STRING ProcessorHardwareIds;
 extern LPWSTR ProcessorIdString;
 extern LPWSTR ProcessorNameString;
 
+#ifndef ACPI_ENUM_CHILDREN_OUTPUT_BUFFER_SIGNATURE
+#define ACPI_ENUM_CHILDREN_OUTPUT_BUFFER_SIGNATURE 'GieA'
+#define ACPI_ENUM_CHILDREN_INPUT_BUFFER_SIGNATURE  'HieA'
+#endif
+
+#ifndef IOCTL_ACPI_ENUM_CHILDREN
+#define IOCTL_ACPI_ENUM_CHILDREN \
+    CTL_CODE(FILE_DEVICE_ACPI, 8, METHOD_BUFFERED, FILE_READ_ACCESS | FILE_WRITE_ACCESS)
+#endif
+
 typedef enum _DEVICE_PNP_STATE {
 
     NotStarted = 0,         // Not started yet
@@ -42,6 +52,7 @@ typedef struct _PDO_DEVICE_DATA
     // An array of (zero terminated wide character strings).
     // The array itself also null terminated
     PWCHAR      HardwareIDs;
+    PWCHAR      InstanceId;
     BOOLEAN     DockDevice;
     // Link point to hold all the PDOs for a single bus together
     LIST_ENTRY  Link;
@@ -100,6 +111,12 @@ Bus_PDO_EvalMethod(
     _In_ PPDO_DEVICE_DATA DeviceData,
     _Inout_ PIRP Irp);
 
+NTSTATUS
+NTAPI
+Bus_PDO_EnumChildren(
+    _In_ PPDO_DEVICE_DATA DeviceData,
+    _Inout_ PIRP Irp);
+
 NTSTATUS
 NTAPI
 Bus_CreateClose (
diff --git a/drivers/bus/acpi/main.c b/drivers/bus/acpi/main.c
index c903635b..f46c30b7 100644
--- a/drivers/bus/acpi/main.c
+++ b/drivers/bus/acpi/main.c
@@ -255,6 +255,14 @@ ACPIDispatchDeviceControl(
                 break;
             }
 
+            case IOCTL_ACPI_ENUM_CHILDREN:
+            {
+                ASSERT(KeGetCurrentIrql() < DISPATCH_LEVEL);
+
+                status = Bus_PDO_EnumChildren((PPDO_DEVICE_DATA)commonData, Irp);
+                break;
+            }
+
            case IOCTL_GET_SYS_BUTTON_CAPS:
               if (irpStack->Parameters.DeviceIoControl.OutputBufferLength < sizeof(ULONG))
               {
diff --git a/drivers/bus/acpi/namespace.c b/drivers/bus/acpi/namespace.c
new file mode 100644
index 00000000..6c4168f1
--- /dev/null
+++ b/drivers/bus/acpi/namespace.c
@@ -0,0 +1,317 @@
+#include "precomp.h"
+
+#define NDEBUG
+#include <debug.h>
+
+#define ACPI_ENUM_POOL_TAG 'nEcA'
+#define ACPI_ENUM_OUTPUT_HEADER_SIZE FIELD_OFFSET(ACPI_ENUM_CHILDREN_OUTPUT_BUFFER, Children)
+#define ACPI_ENUM_RECORD_HEADER_SIZE FIELD_OFFSET(ACPI_ENUM_CHILD, Name)
+#define ACPI_ENUM_KNOWN_FLAGS \
+    (ENUM_CHILDREN_IMMEDIATE_ONLY | ENUM_CHILDREN_MULTILEVEL | ENUM_CHILDREN_NAME_IS_FILTER)
+
+typedef struct _ACPI_ENUM_ENTRY
+{
+    LIST_ENTRY Link;
+    ULONG Flags;
+    ULONG NameLength;
+    CHAR Name[ANYSIZE_ARRAY];
+} ACPI_ENUM_ENTRY, *PACPI_ENUM_ENTRY;
+
+typedef struct _ACPI_ENUM_CONTEXT
+{
+    LIST_ENTRY Entries;
+    NTSTATUS Status;
+    ULONG RequiredLength;
+    ULONG Count;
+    BOOLEAN Filtered;
+    CHAR Filter[ACPI_NAMESEG_SIZE + 1];
+} ACPI_ENUM_CONTEXT, *PACPI_ENUM_CONTEXT;
+
+static
+VOID
+AcpiEnumFreeEntries(
+    PACPI_ENUM_CONTEXT Context)
+{
+    PLIST_ENTRY Link;
+    PACPI_ENUM_ENTRY Entry;
+
+    while (!IsListEmpty(&Context->Entries))
+    {
+        Link = RemoveHeadList(&Context->Entries);
+        Entry = CONTAINING_RECORD(Link, ACPI_ENUM_ENTRY, Link);
+        ExFreePoolWithTag(Entry, ACPI_ENUM_POOL_TAG);
+    }
+}
+
+static
+ACPI_STATUS
+AcpiEnumCaptureObject(
+    ACPI_HANDLE Object,
+    UINT32 NestingLevel,
+    void *RawContext,
+    void **ReturnValue)
+{
+    PACPI_ENUM_CONTEXT Context = RawContext;
+    ACPI_BUFFER Path = {ACPI_ALLOCATE_BUFFER, NULL};
+    ACPI_HANDLE Child;
+    ACPI_STATUS AcpiStatus;
+    PACPI_ENUM_ENTRY Entry;
+    SIZE_T NameLength;
+    SIZE_T AllocationLength;
+    ULONG RecordLength;
+    ULONG NewRequiredLength;
+    CHAR Name[ACPI_NAMESEG_SIZE + 1] = {0};
+    ACPI_BUFFER NameBuffer = {sizeof(Name), Name};
+
+    UNREFERENCED_PARAMETER(NestingLevel);
+    UNREFERENCED_PARAMETER(ReturnValue);
+
+    if (Context->Filtered)
+    {
+        AcpiStatus = AcpiGetName(Object, ACPI_SINGLE_NAME, &NameBuffer);
+        if (ACPI_FAILURE(AcpiStatus))
+        {
+            Context->Status = AcpiStatus == AE_NO_MEMORY ? STATUS_INSUFFICIENT_RESOURCES
+                                                         : STATUS_INVALID_DEVICE_REQUEST;
+            return AE_CTRL_TERMINATE;
+        }
+        if (RtlCompareMemory(Name, Context->Filter, ACPI_NAMESEG_SIZE) != ACPI_NAMESEG_SIZE ||
+            Name[ACPI_NAMESEG_SIZE] != ANSI_NULL)
+        {
+            return AE_OK;
+        }
+    }
+
+    AcpiStatus = AcpiGetName(Object, ACPI_FULL_PATHNAME, &Path);
+    if (ACPI_FAILURE(AcpiStatus) || !Path.Pointer)
+    {
+        Context->Status = AcpiStatus == AE_NO_MEMORY ? STATUS_INSUFFICIENT_RESOURCES
+                                                     : STATUS_INVALID_DEVICE_REQUEST;
+        return AE_CTRL_TERMINATE;
+    }
+
+    NameLength = strlen(Path.Pointer) + sizeof(ANSI_NULL);
+    if (NameLength > MAXULONG ||
+        !NT_SUCCESS(RtlULongAdd(ACPI_ENUM_RECORD_HEADER_SIZE,
+                               (ULONG)NameLength,
+                               &RecordLength)) ||
+        !NT_SUCCESS(RtlULongAdd(Context->RequiredLength,
+                               RecordLength,
+                               &NewRequiredLength)) ||
+        Context->Count == MAXULONG)
+    {
+        AcpiOsFree(Path.Pointer);
+        Context->Status = STATUS_INTEGER_OVERFLOW;
+        return AE_CTRL_TERMINATE;
+    }
+    AllocationLength = FIELD_OFFSET(ACPI_ENUM_ENTRY, Name) + NameLength;
+    if (AllocationLength < NameLength)
+    {
+        AcpiOsFree(Path.Pointer);
+        Context->Status = STATUS_INTEGER_OVERFLOW;
+        return AE_CTRL_TERMINATE;
+    }
+
+    Entry = ExAllocatePoolWithTag(PagedPool, AllocationLength, ACPI_ENUM_POOL_TAG);
+    if (!Entry)
+    {
+        AcpiOsFree(Path.Pointer);
+        Context->Status = STATUS_INSUFFICIENT_RESOURCES;
+        return AE_CTRL_TERMINATE;
+    }
+    AcpiStatus = AcpiGetNextObject(ACPI_TYPE_ANY, Object, NULL, &Child);
+    if (ACPI_FAILURE(AcpiStatus) && AcpiStatus != AE_NOT_FOUND)
+    {
+        ExFreePoolWithTag(Entry, ACPI_ENUM_POOL_TAG);
+        AcpiOsFree(Path.Pointer);
+        Context->Status = AcpiStatus == AE_NO_MEMORY ? STATUS_INSUFFICIENT_RESOURCES
+                                                     : STATUS_INVALID_DEVICE_REQUEST;
+        return AE_CTRL_TERMINATE;
+    }
+    Entry->Flags = ACPI_SUCCESS(AcpiStatus) ? ACPI_OBJECT_HAS_CHILDREN : 0;
+    Entry->NameLength = (ULONG)NameLength;
+    RtlCopyMemory(Entry->Name, Path.Pointer, NameLength);
+    AcpiOsFree(Path.Pointer);
+
+    InsertTailList(&Context->Entries, &Entry->Link);
+    Context->RequiredLength = NewRequiredLength;
+    Context->Count++;
+    return AE_OK;
+}
+
+static
+NTSTATUS
+AcpiEnumCaptureNamespace(
+    ACPI_HANDLE Root,
+    ULONG Flags,
+    PACPI_ENUM_CONTEXT Context)
+{
+    ACPI_STATUS AcpiStatus;
+    ACPI_OBJECT_TYPE Type;
+    UINT32 MaxDepth;
+
+    if (!Context->Filtered)
+    {
+        AcpiStatus = AcpiEnumCaptureObject(Root, 0, Context, NULL);
+        if (ACPI_FAILURE(AcpiStatus))
+            return Context->Status;
+    }
+
+    Type = Context->Filtered ? ACPI_TYPE_ANY : ACPI_TYPE_DEVICE;
+    MaxDepth = (Flags & ENUM_CHILDREN_MULTILEVEL) ? ACPI_UINT32_MAX : 1;
+    AcpiStatus = AcpiWalkNamespace(Type,
+                                   Root,
+                                   MaxDepth,
+                                   AcpiEnumCaptureObject,
+                                   NULL,
+                                   Context,
+                                   NULL);
+    if (ACPI_FAILURE(AcpiStatus))
+    {
+        if (!NT_SUCCESS(Context->Status))
+            return Context->Status;
+        return AcpiStatus == AE_NO_MEMORY ? STATUS_INSUFFICIENT_RESOURCES
+                                         : STATUS_INVALID_DEVICE_REQUEST;
+    }
+    return STATUS_SUCCESS;
+}
+
+static
+BOOLEAN
+AcpiEnumValidNameSeg(
+    const CHAR Name[ACPI_NAMESEG_SIZE + 1])
+{
+    ULONG Index;
+
+    if (!((Name[0] >= 'A' && Name[0] <= 'Z') || Name[0] == '_') ||
+        Name[ACPI_NAMESEG_SIZE] != ANSI_NULL)
+    {
+        return FALSE;
+    }
+    for (Index = 1; Index < ACPI_NAMESEG_SIZE; Index++)
+    {
+        if (!((Name[Index] >= 'A' && Name[Index] <= 'Z') ||
+              (Name[Index] >= '0' && Name[Index] <= '9') ||
+              Name[Index] == '_'))
+        {
+            return FALSE;
+        }
+    }
+    return TRUE;
+}
+
+NTSTATUS
+NTAPI
+Bus_PDO_EnumChildren(
+    _In_ PPDO_DEVICE_DATA DeviceData,
+    _Inout_ PIRP Irp)
+{
+    PIO_STACK_LOCATION IrpStack;
+    PACPI_ENUM_CHILDREN_INPUT_BUFFER Input;
+    PACPI_ENUM_CHILDREN_OUTPUT_BUFFER Output;
+    ACPI_ENUM_CONTEXT Context;
+    ULONG InputLength;
+    ULONG OutputLength;
+    ULONG Flags;
+    ULONG BaseFlags;
+    ULONG Signature;
+    ULONG OutputSignature = ACPI_ENUM_CHILDREN_OUTPUT_BUFFER_SIGNATURE;
+    ULONG NameLength;
+    CHAR Filter[ACPI_NAMESEG_SIZE + 1] = {0};
+    PLIST_ENTRY Link;
+    PACPI_ENUM_ENTRY Entry;
+    PUCHAR Cursor;
+    NTSTATUS Status;
+
+    PAGED_CODE();
+
+    IrpStack = IoGetCurrentIrpStackLocation(Irp);
+    InputLength = IrpStack->Parameters.DeviceIoControl.InputBufferLength;
+    OutputLength = IrpStack->Parameters.DeviceIoControl.OutputBufferLength;
+    Input = Irp->AssociatedIrp.SystemBuffer;
+    Irp->IoStatus.Information = 0;
+    if (!DeviceData->AcpiHandle)
+        return STATUS_INVALID_DEVICE_REQUEST;
+    if (!Input || InputLength < sizeof(ACPI_ENUM_CHILDREN_INPUT_BUFFER))
+        return STATUS_INVALID_PARAMETER;
+
+    Signature = Input->Signature;
+    Flags = Input->Flags;
+    NameLength = Input->NameLength;
+    BaseFlags = Flags & (ENUM_CHILDREN_IMMEDIATE_ONLY | ENUM_CHILDREN_MULTILEVEL);
+    if (Signature != ACPI_ENUM_CHILDREN_INPUT_BUFFER_SIGNATURE ||
+        (Flags & ~ACPI_ENUM_KNOWN_FLAGS) ||
+        (BaseFlags != ENUM_CHILDREN_IMMEDIATE_ONLY &&
+         BaseFlags != ENUM_CHILDREN_MULTILEVEL))
+    {
+        return STATUS_INVALID_PARAMETER;
+    }
+    if (Flags & ENUM_CHILDREN_NAME_IS_FILTER)
+    {
+        if (NameLength != sizeof(Filter) ||
+            InputLength < FIELD_OFFSET(ACPI_ENUM_CHILDREN_INPUT_BUFFER, Name) + NameLength)
+        {
+            return STATUS_INVALID_PARAMETER;
+        }
+        RtlCopyMemory(Filter, Input->Name, sizeof(Filter));
+        if (!AcpiEnumValidNameSeg(Filter))
+            return STATUS_INVALID_PARAMETER;
+    }
+
+    if (OutputLength < ACPI_ENUM_OUTPUT_HEADER_SIZE)
+        return STATUS_BUFFER_TOO_SMALL;
+
+    InitializeListHead(&Context.Entries);
+    Context.Status = STATUS_SUCCESS;
+    Context.RequiredLength = ACPI_ENUM_OUTPUT_HEADER_SIZE;
+    Context.Count = 0;
+    Context.Filtered = !!(Flags & ENUM_CHILDREN_NAME_IS_FILTER);
+    RtlCopyMemory(Context.Filter, Filter, sizeof(Filter));
+
+    Status = AcpiEnumCaptureNamespace(DeviceData->AcpiHandle, Flags, &Context);
+    if (!NT_SUCCESS(Status))
+    {
+        AcpiEnumFreeEntries(&Context);
+        return Status;
+    }
+
+    Output = Irp->AssociatedIrp.SystemBuffer;
+    RtlCopyMemory((PUCHAR)Output + FIELD_OFFSET(ACPI_ENUM_CHILDREN_OUTPUT_BUFFER, Signature),
+                  &OutputSignature,
+                  sizeof(OutputSignature));
+    if (OutputLength < Context.RequiredLength)
+    {
+        RtlCopyMemory((PUCHAR)Output +
+                          FIELD_OFFSET(ACPI_ENUM_CHILDREN_OUTPUT_BUFFER, NumberOfChildren),
+                      &Context.RequiredLength,
+                      sizeof(Context.RequiredLength));
+        AcpiEnumFreeEntries(&Context);
+        return STATUS_BUFFER_OVERFLOW;
+    }
+
+    RtlCopyMemory((PUCHAR)Output +
+                      FIELD_OFFSET(ACPI_ENUM_CHILDREN_OUTPUT_BUFFER, NumberOfChildren),
+                  &Context.Count,
+                  sizeof(Context.Count));
+    Cursor = (PUCHAR)Output + ACPI_ENUM_OUTPUT_HEADER_SIZE;
+    for (Link = Context.Entries.Flink;
+         Link != &Context.Entries;
+         Link = Link->Flink)
+    {
+        Entry = CONTAINING_RECORD(Link, ACPI_ENUM_ENTRY, Link);
+        RtlCopyMemory(Cursor + FIELD_OFFSET(ACPI_ENUM_CHILD, Flags),
+                      &Entry->Flags,
+                      sizeof(Entry->Flags));
+        RtlCopyMemory(Cursor + FIELD_OFFSET(ACPI_ENUM_CHILD, NameLength),
+                      &Entry->NameLength,
+                      sizeof(Entry->NameLength));
+        RtlCopyMemory(Cursor + ACPI_ENUM_RECORD_HEADER_SIZE,
+                      Entry->Name,
+                      Entry->NameLength);
+        Cursor += ACPI_ENUM_RECORD_HEADER_SIZE + Entry->NameLength;
+    }
+    NT_ASSERT((ULONG)(Cursor - (PUCHAR)Output) == Context.RequiredLength);
+    Irp->IoStatus.Information = Context.RequiredLength;
+    AcpiEnumFreeEntries(&Context);
+    return STATUS_SUCCESS;
+}
diff --git a/drivers/bus/acpi/pnp.c b/drivers/bus/acpi/pnp.c
index 9c87bbb0..70b1e8dc 100644
--- a/drivers/bus/acpi/pnp.c
+++ b/drivers/bus/acpi/pnp.c
@@ -415,6 +415,11 @@ Bus_DestroyPdo (
         PdoData->HardwareIDs = NULL;
     }
 
+    if (PdoData->InstanceId) {
+        ExFreePoolWithTag(PdoData->InstanceId, 'IpcA');
+        PdoData->InstanceId = NULL;
+    }
+
     DPRINT("\tDeleting PDO: 0x%p\n", Device);
     IoDeleteDevice (Device);
     return STATUS_SUCCESS;
@@ -605,4 +610,3 @@ DbgDeviceIDString(
 
 #endif
 
-
